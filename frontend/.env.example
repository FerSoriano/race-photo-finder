VITE_API_BASE_URL=http://localhost:8000

# Public origin of the deployed frontend. Only used to build the absolute
# og:image URL in index.html -- WhatsApp will not render a preview card from a
# relative one. Falls back to the dev server origin when unset.
VITE_SITE_URL=http://localhost:5173
